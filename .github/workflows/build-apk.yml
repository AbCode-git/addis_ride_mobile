name: Build Android APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Create .env file
        run: |
          echo "ORS_API_KEY=${{ secrets.ORS_API_KEY }}" > .env

      - name: Install Expo CLI
        run: npm install -g @expo/cli

      - name: Prebuild Android project
        run: npx expo prebuild --platform android --clean

      - name: Grant execute permission for gradlew
        working-directory: ./android
        run: chmod +x ./gradlew

      - name: Build Android APK (arm64 only - faster build)
        working-directory: ./android
        run: ./gradlew assembleRelease -PbuildOnlyArm64=true
        env:
          GRADLE_OPTS: "-Dorg.gradle.jvmargs=-Xmx4g -Dorg.gradle.daemon=false"

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: android/app/build/outputs/apk/release/app-release.apk
          retention-days: 30

      - name: Get APK Info
        run: |
          echo "### ✅ APK Build Complete!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "📦 **File:** app-release.apk" >> $GITHUB_STEP_SUMMARY
          echo "📁 **Download from:** Actions → This workflow run → Artifacts" >> $GITHUB_STEP_SUMMARY
          echo "🔧 **Architecture:** ARM64 (works on most modern Android phones)" >> $GITHUB_STEP_SUMMARY
          ls -lh android/app/build/outputs/apk/release/app-release.apk >> $GITHUB_STEP_SUMMARY || echo "APK file not found" >> $GITHUB_STEP_SUMMARY

